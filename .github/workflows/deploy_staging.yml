name: Deploy into staging

on:
  workflow_dispatch:
    branches:
      - main
    inputs:
      origin_branch:
        required: true
        type: string
        default: 'main'
      merge_branch:
        required: true
        type: string
        default: 'staging'
    secrets:
      PAT:
        required: true

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  call_reusable_workflow:
    uses: eammosov/cicd_actions/.github/workflows/merge_branches.yml@feat/OC-3039/fix
    with:
      origin_branch: ${{ inputs.origin_branch }}
      merge_branch: ${{ inputs.merge_branch }}
    secrets:
      token: "${{ secrets.PAT }}"
